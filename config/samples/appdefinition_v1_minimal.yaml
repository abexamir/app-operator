apiVersion: appdefinition.abexamir.me/v1
kind: AppDefinition
metadata:
  name: my-app
  namespace: default
spec:
  containers:
    - name: web
      image: nginx:1.25-alpine
      ports:
        - name: http
          containerPort: 80
          servicePort: 80
          expose: true
  replicas: 2
