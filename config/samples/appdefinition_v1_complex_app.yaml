apiVersion: appdefinition.abexamir.me/v1
kind: AppDefinition
metadata:
  name: complex-app
  namespace: default
spec:
  containers:
    - name: web
      image: nginx:1.25-alpine
      command: ["nginx"]
      args: ["-g", "daemon off;"]
      ports:
        - name: http
          containerPort: 80
          servicePort: 80
          protocol: TCP
          expose: true
          metricsPath: /metrics
      env:
        - name: NGINX_HOST
          value: "example.com"
        - name: NGINX_PORT
          value: "80"
      resources:
        requests:
          cpu: "100m"
          memory: "128Mi"
        limits:
          cpu: "500m"
          memory: "512Mi"
      readinessProbe:
        type: httpGet
        httpGet:
          path: /health
          port: 80
        initialDelaySeconds: 5
        periodSeconds: 10
        timeoutSeconds: 2
        failureThreshold: 3
        successThreshold: 1
      livenessProbe:
        type: httpGet
        httpGet:
          path: /health
          port: 80
        initialDelaySeconds: 15
        periodSeconds: 20
        timeoutSeconds: 2
        failureThreshold: 3
        successThreshold: 1

    - name: metrics
      image: prom/prometheus:v2.45.0
      command: ["prometheus"]
      args:
        - "--config.file=/etc/prometheus/prometheus.yml"
        - "--storage.tsdb.path=/data"    # write TSDB to the PVC partition
        - "--web.listen-address=:9090"
      ports:
        - name: metrics
          containerPort: 9090
          servicePort: 9090
          protocol: TCP
          expose: true
          metricsPath: /metrics
      resources:
        requests:
          cpu: "50m"
          memory: "64Mi"
        limits:
          cpu: "200m"
          memory: "256Mi"
      readinessProbe:
        type: httpGet
        httpGet:
          path: /-/ready
          port: 9090
        initialDelaySeconds: 5
        periodSeconds: 10
      livenessProbe:
        type: httpGet
        httpGet:
          path: /-/healthy
          port: 9090
        initialDelaySeconds: 15
        periodSeconds: 20

  replicas: 1    # Prometheus requires a single writer for its TSDB; multiple replicas cause lock conflicts.

  domains:
    - name: app.example.com
      path: /
      tls: true
      redirect_tls: true
      certIssuer: letsencrypt-prod
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
        nginx.ingress.kubernetes.io/ssl-redirect: "true"
    - name: metrics.app.example.com
      path: /
      tls: true
      certIssuer: letsencrypt-prod
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod

  disk:
    sizeInGi: 10
    storageClassName: local-path
    setFsGroup: true
    partitions:
      - mountPath: /data
        subPath: data
      - mountPath: /logs
        subPath: logs

  configMaps:
    - name: nginx-config
      mountPath: /etc/nginx/conf.d
      data:
        default.conf: |
          server {
            listen 80;
            server_name _;
            location / { root /usr/share/nginx/html; index index.html; }
            location /health { return 200 "ok\n"; add_header Content-Type text/plain; }
          }
    - name: prometheus-config
      mountPath: /etc/prometheus
      data:
        prometheus.yml: |
          global:
            scrape_interval: 30s
          scrape_configs:
            - job_name: 'web'
              static_configs:
                - targets: ['localhost:80']

  secrets:
    - name: app-credentials
      asEnvVars: true
      data:
        DB_PASSWORD: "super-secret-password"
        API_KEY: "my-api-key-12345"

  loggingConfig:
    stdout:
      enabled: true
      format: json
    stderr:
      enabled: true
      format: json
    files:
      - path: /logs/app.log
        format: json
        multilinePattern: "^\\d{4}-\\d{2}-\\d{2}"

  monitoringConfig:
    enabled: true
    scrapeInterval: "30s"
    labels:
      app.kubernetes.io/name: complex-app
      app.kubernetes.io/instance: complex-app
      app.kubernetes.io/component: web

  securityContext:
    runAsNonRoot: false
    fsGroup: 65534    # nobody group — ensures PVC is group-writable for prometheus (UID 65534)

  serviceType: ClusterIP

  ingressAnnotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "300"
